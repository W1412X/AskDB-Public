# TODO

待办与规划项，按优先级/类型列出。

## 高优先级

- [ ] **Web UI 能力补齐**
  - 已有 Vue 3 + FastAPI 基础界面（配置 / 初始化 / 问答）；可继续增强：会话历史、多库切换、错误提示与重试体验等。

- [ ] **执行图可视化增强**
  - 前端已可用 Vue Flow 等展示工作流视图；可进一步：与 `build_query_snapshot` 字段对齐、高亮当前节点、导出 Mermaid。

## 中优先级

- [ ] **请求级日志与审计的查询/导出**
  - 对 `log/request_*.log` 或审计结构提供按 request_id / workflow_id 查询、导出为报告或结构化数据。

- [ ] **配置校验与启动前检查**
  - 启动时校验 `config/json/*.json` 必填项与数据库连通性，失败时给出明确提示。

- [ ] **单元测试与集成测试补充**
  - 为 query_workflow、schemalink、repair loop 等关键路径补充/稳定化测试，便于 CI。

## 低优先级 / 可选

- [ ] **多数据源 / 多方言**
  - 在现有 MySQL 基础上，支持更多数据库类型或 SQL 方言（配置化 dialect）。

- [ ] **Embedding 模型缓存与预拉取**
  - 首次使用前检测并提示下载 embedding 模型，或提供离线包说明。

- [ ] **文档与示例**
  - 补充端到端示例（从建库到查询）、常见问题与故障排查。

---

欢迎在 Issue 或 PR 中补充、认领或调整上述项。
