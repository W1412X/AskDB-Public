# 复制为 .env 并填入真实值，勿将 .env 提交到版本库
# cp .env.example .env

# ----- 数据库（对应 database.json 中 connection.password_env） -----
DB_PASSWORD=

# ----- LLM API Key（对应 models.json 里 providers.*.api_key_env，按供应商一份密钥） -----
OPENAI_API_KEY=
QWEN_API_KEY=
DEEPSEEK_API_KEY=


# ----- 可选：自定义配置目录（默认使用 config/json/） -----
# APP_CONFIG_DIR=/path/to/your/config